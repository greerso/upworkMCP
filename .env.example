# Copy to .env or use wrangler secrets for production
# These are only needed for local testing if you modify code to read process.env (not the case here - we use wrangler secrets + bindings)

# UPWORK_CLIENT_ID=your-client-id-from-upwork-dev-portal
# UPWORK_CLIENT_SECRET=your-client-secret

# For local KV simulation wrangler handles it.
